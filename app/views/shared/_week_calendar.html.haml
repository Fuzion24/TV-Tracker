%table{:class =>'table table-bordered table-striped'}
  %thead
    %tr
    -(-3..3).each do |day|
      %th{:style => "text-align: center;"}
        =UserHelper.day_of_week(day)
        <br/>
        =day == 0 ? "(Today)" : UserHelper.numeric_date(day)

  %tbody
    %tr
      - (-3..3).each do |offset| 
        %td{:style => "text-align: center;"}
          - UserHelper.episodestoday(@user_shows, Date.today + offset).each do |tv_episode|
            =render :partial => 'shared/tv_show_pill', :locals => {:showName => tv_episode.tv_season.tv_show.name, :episodeName => tv_episode.name, :airtime => tv_episode.tv_season.tv_show.air_time} 
            %br

