<h1>TvEpisodes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Network</th>
    <th>Premiered</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tv_episodes.each do |tv_episode| %>
  <tr>
    <td><%= tv_episode.name %></td>
    <td><%= tv_episode.description %></td>
    <td><%= tv_episode.airdate %></td>
    <td><%= link_to 'Show', tv_episode %></td>
    <td><%= link_to 'Edit', edit_tv_episode_path(tv_episode) %></td>
    <td><%= link_to 'Destroy', tv_episode, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tv show', new_tv_episode_path %>
